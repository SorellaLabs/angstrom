--- crates/eth/src/manager.rs
+++ replace <impl Future for EthDataCleanser<Sync>>::poll -> Poll<Self::Output> with Poll::new()
@@ -284,33 +284,17 @@
 impl<Sync> Future for EthDataCleanser<Sync>
 where
     Sync: BlockSyncProducer
 {
     type Output = ();
 
     fn poll(mut self: std::pin::Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> {
         // poll all canonical updates
-        while let Poll::Ready(is_some) = self.canonical_updates.poll_next_unpin(cx).map(|res| {
-            res.transpose()
-                .ok()
-                .flatten()
-                .map(|update| self.on_canon_update(update))
-                .is_some()
-        }) {
-            if !is_some {
-                return Poll::Ready(())
-            }
-        }
-
-        while let Poll::Ready(Some(command)) = self.commander.poll_next_unpin(cx) {
-            self.on_command(command)
-        }
-
-        Poll::Pending
+        Poll::new() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Debug, Clone)]
 pub enum EthEvent {
     //TODO: add shit here
     NewBlock(u64),
     NewBlockTransitions {
